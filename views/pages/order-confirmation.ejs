<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>order confirmed</title>
</head>
<body>
    <div>
        <h1>Thank you for placing order!</h1>
    </div>
    
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmed</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: url('images/blackfooter.jpg') no-repeat center center/cover;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .confirmation-container {
            width: 90%;
            /* height: 100px; */
            max-width: 800px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 50px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            text-align: center;
            color: white;
            animation: fadeIn 1.5s ease-in-out;
        }

        .confirmation-header {
            font-size: 2rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            color: #ffffff;
            margin-bottom: 30px;
        }

        .confirmation-header img {
            width: 40px;
            height: 40px;
            animation: tickScale 1.2s ease;
        }

        .message {
            background: rgba(0, 0, 0, 0.3);
            padding: 30px;
            border-radius: 10px;
            font-size: 1.5rem;
            line-height: 1.8;
            animation: slideUp 1s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes slideUp {
            from {
                transform: translateY(30px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes tickScale {
            0%, 50% {
                transform: scale(0.5);
            }
            100% {
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="confirmation-container">
        <div class="confirmation-header">
            Confirmed
            <img src=images/tick-inside-circle.png  alt="Checkmark">
        </div>
        <div class="message">
            THANK YOU! <br> Your order is confirmed and will be delivered soon.
        </div>
    </div>
</body>
</html>
